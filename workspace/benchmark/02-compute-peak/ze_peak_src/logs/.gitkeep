# ze_peak 原始 stdout 日志目录
#
# `*.log` / `ze_peak_src/logs/*` 已在 ../../.gitignore 里排除（日志是可复现的派生物），
# 保留本文件只是为了让空目录结构在版本库里可见。
#
# 复现方式（logs/ 下的文件名与对应命令）：
#   ze_peak_dev0.log            ./build/ze_peak -d 0 -a -i 50 -w 10
#   ze_peak_dev1.log            ./build/ze_peak -d 1 -a -i 50 -w 10   ← ⚠️ 撞上 DVFS 漂移
#   ze_peak_dev1_rerun.log      ./build/ze_peak -d 1 -a -i 50 -w 10   （干净重跑）
#   ze_peak_dev1_dp_rerun.log   ./build/ze_peak -d 1 -t dp_compute  -i 10 -w 3
#   ze_peak_dev1_int_rerun.log  ./build/ze_peak -d 1 -t int_compute -i 10 -w 3
#   ze_peak_short_dev{0,1}_rep{1,2}.log
#                               ./build/ze_peak -d {0,1} -a -i 3 -w 1  （重复性口径）
#
# ⚠️ `-i 50 -w 10` 的整轮约 20 min/卡，期间 GPU 会 DVFS 漂移
#    （gt_act_freq_mhz 实测出现 1350/1250/800/350 MHz），fp64 / int32 段会偏低。
#    绝对值请引用短跑或分项结果 —— 详见 ../README.md §7。
